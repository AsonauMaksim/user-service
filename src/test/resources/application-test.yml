spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db_test
    username: test_user
    password: test_password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  cache:
    type: redis

  liquibase:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379