spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/user_db_test
    username: test_user
    password: test_password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  cache:
    type: redis

  liquibase:
    enabled: true

  data:
    redis:
      host: redis
      port: 6379

